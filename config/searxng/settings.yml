use_default_settings: true

server:
  # False Positive: Base64 High Entropy String
  secret_key: "__SEARXNG_SECRET__" # checkov:skip=CKV_SECRET_6: placeholder replaced at runtime

search:
  formats:
    - html
    - json
